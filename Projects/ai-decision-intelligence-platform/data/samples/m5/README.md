# Synthetic M5-shaped sample

The CSV files in this directory are generated by `scripts/generate_m5_sample.py`.
They reproduce the required M5 file shapes and identifiers for pipeline testing,
but contain entirely synthetic values. They are not extracted from, or claimed
to represent, Walmart or the original M5 competition data.
